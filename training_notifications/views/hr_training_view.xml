<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="employee_incompleted_training_action" model="ir.actions.act_window">
        <field name="name">Employee evaluation</field>
        <field name="res_model">hr.training</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
        <field name="view_mode">tree,form,kanban</field>
        <field name="view_id" ref="Training_Management.training_tree_view"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No projects found. Let's create one!
            </p>
            <p>
                Projects regroup tasks on the same topic and each have their own dashboard.
            </p>
        </field>
    </record>

    
</odoo>